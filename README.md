This is an ALX Project on building your own custom shell.
This shell is able to display a prompt, tokenize and receive the users input.
Also, it can execute commands, display erros and also exit the shell
